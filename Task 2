
#include <iostream>
using namespace std;

int main()
{
    float n, a, summa = 0;
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        cin >> a;
        summa += a;
    }
    cout << "CA: " << summa / n;
    system("pause");
}
